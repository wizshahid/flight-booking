export enum AirlineStatus {
  Active = 'Active',
  InActive = 'InActive',
}

export enum ScheduledDays {
  Daily,
  WeekDays,
  WeekEnds,
  SpecificDays,
}

export enum Meals {
  None,
  Veg,
  NonVeg,
  Both,
}

export enum Gender {
  Male,
  Female,
}
